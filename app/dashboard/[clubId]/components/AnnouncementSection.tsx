import React from "react";

const AnnouncementSection = ({ announcements }: { announcements: any[] }) => {
  return <p className="text-muted-foreground">No announcements yet</p>;
};
export default AnnouncementSection;
